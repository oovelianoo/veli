# Optional assets

Keep experimental or seasonal profile visuals here. The production README does
not depend on this directory, so alternate hero treatments can be tested without
touching the active identity system.
